# Security policy

Do not report vulnerabilities through public issues.

ModelFit treats uploaded files, retrieved web content, model output, and MCP
tool output as untrusted. New MCP servers must be allowlisted, version-pinned,
permission-scoped, and reviewed before they are enabled in production.
