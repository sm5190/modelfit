# Prompt registry

Store prompts as versioned YAML files. Every production run must record:

- prompt ID and version
- rubric ID and version
- candidate model ID and revision
- judge model ID and revision
- inference parameters
- tool configuration
- runtime environment identifier
